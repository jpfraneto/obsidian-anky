{
  "name": "anky-obsidian-plugin",
  "version": "1.0.0",
  "description": "8-minute forward-only writing. your words, preserved forever.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --external:obsidian --format=cjs --platform=node --outfile=main.js"
  },
  "dependencies": {
    "obsidian": "latest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "^0.17.0",
    "@types/node": "^16.0.0"
  },
  "license": "MIT"
}
